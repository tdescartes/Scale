/**
 * API Configuration
 * Now using Next.js API routes - no external backend needed
 */

export const API_CONFIG = {
  apiUrl: '',  // Empty string = same origin, uses Next.js API routes
  wsUrl: '',   // Not used anymore, using SSE instead
};
